# SPDX-License-Identifier: AGPL-3.0-or-later
# trace returns the complete source-function graph as a nested native strand.

let quarter_step = (value) =>
add(index(7, value), orient(1, value))

output trace(quarter_step) as pattern
