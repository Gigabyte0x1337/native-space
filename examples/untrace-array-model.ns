# SPDX-License-Identifier: AGPL-3.0-or-later
# Generated by untrace from exact ordered observations 1 through 7.
# Mode: deterministic.
# Grammar: homogeneous constant-coefficient linear recurrence.
# Order: 2. Exact held-out matches: 3 of 3.
# First unseen prediction at index 8: add(index(1, 21), index(1, index(1, 55))).

let next_value = (previous_1, previous_2) =>
add(previous_1, previous_2)

let continuation = (position, previous_1, previous_2) =>
continuation(add(position, one), next_value(previous_1, previous_2), previous_1)

let start_continuation = () =>
continuation(3, add(index(1, one), index(1, index(1, 3))), add(index(1, one), index(1, index(1, 2))))

output trace(start_continuation) as pattern
