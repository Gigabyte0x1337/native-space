# SPDX-License-Identifier: AGPL-3.0-or-later
# Shift x by +1 and y by -1; a z coordinate passes through unchanged.

let step = (point) =>
add(point, index(1, 1), index(1, index(1, orient(2, 1))))

output 0
